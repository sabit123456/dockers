pipeline {
 agent any
 stages {
  stage('Compile stage') {
   steps {
    echo "Hello"
   }
  }	
  stage('Test stage') {
   steps {
    echo "Hello New"
   }  
  }
  stage('Deploy stage') {
   steps {
    echo "Hello New Stage added."
   }
  }		
 }
}
